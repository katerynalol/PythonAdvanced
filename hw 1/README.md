# Flask Homework

## Description
Basic Flask application with two routes:
- `/` — returns "Hello, Flask!"
- `/user/<name>` — returns a greeting with the provided name

## How to run
```bash
pip install flask
python main.py
